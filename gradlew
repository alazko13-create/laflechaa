#!/usr/bin/env sh

# Placeholder wrapper script.
# Open this project in Android Studio and generate/update Gradle wrapper if needed.
# Or replace with a standard gradlew from a new Android Studio project.

echo "Open in Android Studio and sync project. If you need CLI builds, generate Gradle wrapper from Android Studio first."
exit 1
